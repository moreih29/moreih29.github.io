---
title: "React Hooks 핵심 정리 (3) - Custom Hook"
slug: react-hooks-3-custom
date: 2026-03-25
description: "반복되는 로직을 Custom Hook으로 추출하여 재사용하는 방법을 알아봅니다."
tags: ["react", "hooks", "custom-hook"]
published: true
series: "React Hooks 핵심 정리"
seriesOrder: 3
category: "Frontend"
---

## Custom Hook이란?

Custom Hook은 `use`로 시작하는 함수로, 여러 Hook을 조합하여 **재사용 가능한 로직**을 만듭니다.

## 예시: useToggle

```tsx
function useToggle(initialValue = false) {
  const [value, setValue] = useState(initialValue)
  const toggle = () => setValue(prev => !prev)
  return [value, toggle] as const
}
```

사용:

```tsx
function Modal() {
  const [isOpen, toggleOpen] = useToggle()

  return (
    <>
      <button onClick={toggleOpen}>
        {isOpen ? '닫기' : '열기'}
      </button>
      {isOpen && <div>모달 내용</div>}
    </>
  )
}
```

## 예시: useLocalStorage

```tsx
function useLocalStorage<T>(key: string, initialValue: T) {
  const [value, setValue] = useState<T>(() => {
    const stored = localStorage.getItem(key)
    return stored ? JSON.parse(stored) : initialValue
  })

  useEffect(() => {
    localStorage.setItem(key, JSON.stringify(value))
  }, [key, value])

  return [value, setValue] as const
}
```

## 정리

| Hook | 역할 |
|------|------|
| `useState` | 상태 관리 |
| `useEffect` | 사이드 이펙트 |
| `Custom Hook` | 로직 재사용 |

> 이것으로 React Hooks 핵심 정리 시리즈를 마칩니다.
